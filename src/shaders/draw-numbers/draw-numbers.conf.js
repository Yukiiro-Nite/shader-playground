import fragmentShader from './draw-numbers.glsl'
import view from './draw-numbers.html';

const config = {
  name: 'Draw Numbers',
  frag: fragmentShader,
  uniforms: {
    
  },
  updates: {

  },
  html: view
}

export default config