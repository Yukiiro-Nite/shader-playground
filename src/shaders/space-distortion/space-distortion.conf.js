import fragmentShader from './space-distortion.glsl'
import view from './space-distortion.html'

const config = {
  name: 'Space Distortion',
  frag: fragmentShader,
  html: view
}

export default config